# 本仓开发约定

本仓同时维护玩家使用的“第五人格启动器”和单独构建的维护者工具箱；`gameRunnerApp/` 是内嵌运行器模板。源码目录更名不自动更改已安装 App、bundle ID、TCC/Keychain 身份或用户数据目录。涉及这些兼容契约时，先查现有升级/恢复行为并保留决定依据。

**同一变更维护地图**：新增、删除或改名顶层对象，改变文件夹职责、对人使用的入口，或改变脚本运行后会构建、打开、安装、提权、改运行态的实际效果时，同步更新 [`projectMap.md`](projectMap.md) 与相关 README 链接。普通函数内部实现调整不需要改地图。地图面向第一次接触项目的读者，说明源码、产物和运行态的区别；生成缓存只写用途，不逐文件列举。

非显然修改应在代码注释、提交正文或 `docs/` 的工程记录中说明问题、原因、方案取舍、适用条件、验证和剩余边界。公开文档与提交不得夹带个人原始日志、截图、账号、凭据、本机私人路径或指向私人档案原文的链接；可公开的工程依据应独立可读。密钥和公证凭据留 Keychain/owner-only 存储。

构建、签名、公证、安装和真实游戏回归是不同验证层。运行构建脚本前先看目标输出目录；除非任务明确要求，不因源码变更更新 `/Applications`、runtime、活动游戏或系统授权。测试通过后记录所证明的边界，发行前仍按最终候选做首次安装和实机审查。
