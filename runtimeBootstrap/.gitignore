# This independent product repository tracks four exact, signed releasePayloads
# as build inputs. Never put the upstream DMG, full Wine runtime, or local caches
# in this directory; see releasePayloads/README.md and the root .gitignore.
