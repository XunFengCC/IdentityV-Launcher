# 本仓开发约定

本仓同时维护玩家使用的“第五人格启动器”和单独构建的维护者工具箱；`gameRunnerApp/` 是内嵌运行器模板。源码目录更名不自动更改已安装 App、bundle ID、TCC/Keychain 身份或用户数据目录。涉及这些兼容契约时，先查现有升级/恢复行为并保留决定依据。

**同一变更维护入口**：新增、删除或改名顶层对象，改变文件夹职责、对人使用的入口，或改变脚本运行后会构建、打开、安装、提权、改运行态的实际效果时，同步更新 [`projectMap.md`](projectMap.md)、[开发者指南](docs/developerGuide.md)中受影响的关系/步骤与相关链接；普通函数内部实现调整不需要改地图。地图是目录和脚本职责索引，开发者指南解释模块关系与维护顺序；面向玩家的 [README](README.md) 只保留产品、安装使用与支持状态。生成缓存只写用途，不逐文件列举。版本或玩家可见行为变化时，同步[变更记录](CHANGELOG.md)，明确候选、已验和未发布，不从旧脚本名推断版本。

非显然修改应在代码注释、提交正文或 `docs/` 的工程记录中说明问题、原因、方案取舍、适用条件、验证和剩余边界。发布前，将拟公开的设计、变更与排错记录补齐背景和版本条件，区分已证实、已推翻与仍未知的判断，保留重要取舍、失败路径和可公开验证依据，使外部维护者能接手并避免重复踩坑。复用现有归属，不强制文档份数或重复完整叙事。公开文档与提交不得夹带个人原始日志、截图、账号、凭据、本机私人路径或指向私人档案原文的链接；可公开的工程依据应独立可读。密钥和公证凭据留 Keychain/owner-only 存储。

构建、签名、公证、安装和真实游戏回归是不同验证层。运行构建脚本前先看目标输出目录；除非任务明确要求，不因源码变更更新 `/Applications`、runtime、活动游戏或系统授权。测试通过后记录所证明的边界，发行前仍按最终候选做首次安装和实机审查。
